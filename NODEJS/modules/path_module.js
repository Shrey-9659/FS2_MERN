const path = require("path")

// Gives complete/absolute path of a file or dir
const pathModule = path.resolve("path_module.js")

// Give the file's basename
// console.log(path.basename(pathModule))
// Gives the directory name
// console.log(path.dirname(pathModule))
// Gives the extention of the file
// console.log(path.extname(pathModule))
// Joins the complete path
// console.log(path.join("/users", "shrey", "example.txt"))