// Rayn dahl
// Node.js -> uses google's V8 Engine (Chrome) to execute JS outside of the browser
// It introduced a non-blocking, event driven I/O model which was perfect for handling multiple requests at once without slowing down the process.

// Node.js : It is an open source, cross-platform JS runtime environment that lets you run JS code on the server.
// Node.js -> run time env

// Major features -> 
// Fast execution, event drive, scalable, npm, same language everywhere.

// Limitation : 
// Not great for CPU-heavy task
// Callback hell
// Single-threaded